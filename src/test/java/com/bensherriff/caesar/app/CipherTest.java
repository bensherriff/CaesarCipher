package com.bensherriff.caesar.app;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class CipherTest {

//    @Test
    public void encryptTest() {
//        assertEquals("yjxy", Cipher.getInstance().encrypt("test", 5));
//        assertEquals("yjxy", Cipher.getInstance().encrypt("test", -5));
//        assertEquals("test", Cipher.getInstance().encrypt("test", 0));
//        assertEquals("test", Cipher.getInstance().encrypt("test", 26));
//        assertEquals("xiwx", Cipher.getInstance().encrypt("test", 30));
//        assertEquals("xiwx", Cipher.getInstance().encrypt("test", -30));
    }

//    @Test
    public void decryptTest() {
//        assertEquals("test", Cipher.getInstance().decrypt("yjxy", 5));
//        assertEquals("test", Cipher.getInstance().decrypt("yjxy", -5));
//        assertEquals("test", Cipher.getInstance().decrypt("test", 0));
//        assertEquals("test", Cipher.getInstance().decrypt("test", 26));
//        assertEquals("test", Cipher.getInstance().decrypt("xiwx", 30));
//        assertEquals("test", Cipher.getInstance().decrypt("xiwx", -30));
    }
}
